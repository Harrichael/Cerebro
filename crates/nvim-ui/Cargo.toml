[package]
name = "nvim-ui"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "A Neovim, drawn into a ratatui Buffer: runs `nvim --embed --headless` and speaks its UI protocol."

[dev-dependencies]
tempfile = { workspace = true }

[dependencies]
anyhow = { workspace = true }
ratatui = "0.30"
rmpv = "1"
